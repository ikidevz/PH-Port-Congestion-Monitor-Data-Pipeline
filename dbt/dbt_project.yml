name: ph_port_monitor
version: "1.0.0"
config-version: 2

profile: ph_port_monitor

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ph_port_monitor:
    staging:
      +materialized: incremental
      +schema: staging
    marts:
      +materialized: incremental
      +schema: marts
